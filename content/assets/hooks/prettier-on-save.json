{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(jq -r '.tool_input.file_path // empty'); [ -n \"$file\" ] || exit 0; case \"$file\" in *.js|*.jsx|*.ts|*.tsx|*.mjs|*.cjs) npx --no-install prettier --write \"$file\" >/dev/null 2>&1 || true ;; esac",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
